# Python 3.13.5
#
# Pinned to the exact versions the committed results in results_main/ and
# results_ablation_*/ were produced with (macOS 15, Apple M1 Pro, CPU).
# Newer versions will very likely work, but PyTorch does not guarantee
# bit-identical results across releases, so reproducing the committed numbers
# exactly means matching these.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   python run_experiment.py --mode smoke_test

torch==2.8.0
numpy==2.1.3
matplotlib==3.10.0
